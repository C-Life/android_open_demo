package com.het.open.lib.biz.api;

import com.het.open.lib.callback.ICallback;

/**
 * Auth逻辑
 * Created by xuchao on 2016/3/14.
 */
public class PhoneAuthDeviceApi {

    /**
     * 请求手机账号所有设备的授权
     * @param phone	    是	string	待授权的手机号([手机号+密钥]，再MD5加密)
     * @param authOpenId	是	string	授权的openId
     * @param openId	    是	string	待授权的openId
     */
    public static  void getAllAuthDevice(ICallback<String> callback, String phone,String authOpenId,String openId){

//        TreeMap<String, String> param = new TreeMap<>();
//        param.put("phone", phone);
//        param.put("authOpenId", authOpenId);
//        param.put("openId", openId);
//        String url=GlobalAddr.HOST;
//        if (url.contains("dp")||url.contains("50")){
//            url=url+"v1/app/open/";
//        }
//        new HetNetworkBuilder(new HetBaseNetwork()).setCallBack(callback)
//                .setParams(param).setUrl(url+"v1/auth/getAllAuthDevice").setMethod(Request.Method.POST).setId(id).setSign().setHttps().commit();

    }




}
